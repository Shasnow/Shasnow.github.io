## 项目简介
本项目为 [StarRailAssistant](https://github.com/Shasnow/StarRailAssistant) 的官方网站提供支持，基于 Starlight 框架。

相关链接

[StarRailAssistant](https://starrailassistant.top)

[![Built with Starlight](https://astro.badg.es/v2/built-with-starlight/tiny.svg)](https://starlight.astro.build)


## 🚀 快速开始

## 如果需要修改文档内容，可直接定位到对应的md文件，修改后保存即可。

### 1️⃣ 安装依赖
确保你的机器上安装了 **Node.js (>=18.x)**，然后运行：
```sh
npm install  # 或者使用 pnpm install / yarn install
```
> 如果你看到了类似 `npm error While resolving: vuepress-theme-hope@2.0.0-rc.92` 的错误，可以尝试用 `npm install @vuepress/plugin-docsearch@2.0.0-rc.108 --save-dev` 解决。


### 本地运行
```sh
npm run dev  # 或 pnpm run dev / yarn dev
```
访问 [http://localhost:4321](http://localhost:4321) 即可预览文档。

### 生成静态文件
```sh
npm run build  # 或 pnpm run build / yarn build
```
静态文件将生成到 `./dist/` 目录。

### 预览构建结果
```sh
npm run preview  # 或 pnpm run preview / yarn preview
```


## ❤️ 贡献指南

欢迎贡献你的改进！
1. **Fork** 本仓库
2. **创建分支**
   ```sh
   git checkout -b feature/my-new-feature
   ```
3. **提交修改**
   ```sh
   git commit -m "feat: 添加新功能"
   ```
4. **推送分支**
   ```sh
   git push origin feature/my-new-feature
   ```
5. **创建 Pull Request**


## 📜 许可证
本项目采用 MIT License 许可。