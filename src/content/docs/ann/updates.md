---
title: 更新公告
description: SRA 各版本更新内容与问题修复记录。
sidebar:
  order: 1
---

## 2.21 更新公告

### 主要更新内容:
- 货币战争中当战斗超时时，强制放弃并结算。
- 新增SRA MCP (Model Control Protocol) 服务器，为Agent接入准备好。
- 新增operator命令，用于直接调用SRA的Operator API以实现游戏操控。
- 不再单独分发SRA ServerDLC，而是包含在主程序包中。
- 升级到Avalonia 12。
- 更新更新解压失败后的错误处理逻辑，增加手动解压选项。
- 取消了清体力“饰品提取”流程中的存档检查。
- 调整了清体力“历战余响”的自动检测逻辑，现在当它启用自动检测时视为挑战3次。

### 问题修复：
- 修复了货币战争中无法处理命运卜者事件的问题。 #234
- 修复了货币战争中自动战斗情况下前后台角色均无法造成伤害时，卡死在奖励关卡的问题。 #233
- 修复了差分宇宙/货币战争中当点击前往参与后未直接进入主界面导致任务无法正常运行的问题。
- 修复了自动对话无法跳过对话的问题。
- 修复了云游戏模式下无法切换账号的问题。
- 修复了货币战争中特殊情况下无法输入攻略码的问题。
- 修复了差分宇宙选择面具时有刷新按钮导致无法选择面具的问题。
- 修复了货币战争中当选择了阿哈大悦策略后卡死的问题。
- 修复了国际服无法自动登录的问题。
- 修复了货币战争标准/超频博弈任务结束后崩溃的问题。
- 修复了b服无法自动登录的问题。
- 修复了清体力多倍活动检测中设置副本目标后无法取消设置的问题。
- 如果你在更新后发现问题依然存在，请及时反馈给我们。

### 更新说明：

#### 新增operator命令
- 新增operator命令，用于直接调用SRA的Operator API以实现游戏操控。
- operator list - 显示所有可用的Operator API。
- operator call <method> <params> - 调用指定的Operator API，参数为JSON格式。
- operator help <method> - 显示指定Operator API的详细帮助信息。
- SRA Server同步更新。

#### 内置SRA ServerDLC
- 为了减少用户下载时的疑惑和提供统一的使用体验，不再单独分发SRA ServerDLC，而是包含在主程序包中。
- 但要使用SRA-server，你必须安装[ASP.NET Core 运行时 10.0](https://dotnet.microsoft.com/zh-cn/download/dotnet/10.0)。或[.NET SDK 10.0](https://dotnet.microsoft.com/download/dotnet/10.0)。

#### SRA MCP服务器

- 在此版本中新增了SRA MCP服务器，为agent接入提供了基础支持。
- 启动SRA Server后，在Agent中配置`http://host:port/mcp`，并以流式HTTP方式访问即可。
- 服务器提供了一组精选的MCP工具，用于与SRA或游戏本身进行交互。这些工具包括但不限于：
  - 获取游戏截图与OCR识别
  - 模拟输入
  - 任务列表
  - 其他自定义工具

## 2.20 更新公告

#### 主要更新内容:
- 添加了新的特殊背景"yumemizukimizuki"
- 新增游戏活动日历功能，支持日历视图展示版本活动信息，按日期筛选活动并显示剩余时间倒计时。
- 更新解压流程优化：手动更新时新增确认解压对话框，避免误操作；自动更新沿用原流程。
- 新增拓展系统，支持自定义插件和脚本。
- 移除了旧版触发器系统，现在由拓展系统替代。
- 默认禁用任务失败后重试功能。
- 支持国际服自动登录前选择区服 #232 @Justice-ocr
- 清开拓力中任务清单与培养目标不再互斥。

#### 问题修复：
- 修复了修改设置后需要重启SRA才能生效的问题。
- 修复了侵蚀隧洞中迅拳之径与霜风之径丢失的问题。
- 修复了当货币战争未选择攻略时导致任务失败的问题。
- 修复了货币战争金币数量识别错误导致无限进行升级操作的问题。
- 修复了货币战争中遇到命运圣杯事件导致任务失败的问题。
- 修复了差分宇宙结算时检测超时的问题。
- 修复了领取签证奖励时偶现卡加载导致任务失败的问题。
- 修复了任务结束后通知截图丢失的问题。
- 修复了当切换其他配置时，多倍活动检测的目标副本选项未刷新的问题。

#### 更新说明：

###### 清体力逻辑变更
- 清体力逻辑变更，任务清单与培养目标不再互斥。
- 具体的副本添加顺序：由多倍活动检测添加的 -> 由培养目标添加的 -> 由任务清单添加的。

###### 拓展系统：
- 新增拓展系统，支持自定义插件和脚本。
- 拓展系统旨在以最小的侵入或对主程序的修改，实现动态加载自定义插件和脚本的功能。
- 只需按固定模式编写python脚本，即可在SRA中动态加载和运行，自动生成可视化配置界面。

###### 破坏性变更：
- 移除了旧版触发器系统，现在由拓展系统替代。
- trigger及它的子命令已被移除，现在由拓展系统替代。
- 示例：
  - 旧 trigger enable \<extension\_id\>
  - 新 extension run \<extension\_id\>
  - 旧 trigger disable \<extension\_id\>
  - 新 extension stop \<extension\_id\>

---

## 2.19 更新公告

#### 主要更新内容:
- 货币战争现在支持在攻略中指定特殊事件处理策略。
- SRA-cli现在支持动态新增cli命令。
- 清体力副本数据现在使用统一的数据源。

#### 问题修复：
- 修复了货币战争中当选择了免战牌投资策略后，无法识别到出战按钮导致卡死的问题。
- 修复了货币战争中无法识别领航员特殊事件的问题。
- 修复了货币战争中无法识别命运圣杯特殊事件的问题。
- 修复了货币战争刷开局中当选择投资策略连续触发时误认为进入第二位面的问题。
- 修复了"万色，愚者自将"无法识别的问题。
- 修复了onebot通知渠道丢失图片的问题。
- 修复了偶现无法识别到月卡界面的问题。
- 修复了差分宇宙偶现积分奖励已满仍继续任务的问题。
- 修复了差分宇宙无法领取奇迹的问题。

#### 更新说明：

###### 在攻略中指定特殊事件处理策略
- 货币战争攻略新增 `special_events` 字段，用于指定特殊事件的选择。
- 支持的特殊事件有：`领航员`、`盛会之星`、`头号玩家`、`命运卜者`。
- 格式：`特殊事件名称`: `选择`，选择为选项中的视觉文本。
```json5
{
  "special_events": {
    "领航员": "护盾",
    "盛会之星": "星期日",
    "头号玩家": "银狼",
    "其他事件": "abc def"
  }
}
```

###### 动态新增cli命令
- 基于SRA-cli的任务扫描功能，现在SRA-cli可以动态新增cli命令
- 本次更新新增了货币战争相关的cli命令，包括：
  - `strategy list` - 获取已安装的货币战争策略列表
  - `tpconfig` - 获取清体力副本数据
- 通过修改 `taskcli.py` 可以新增更多命令。

###### 清体力副本数据使用统一的数据源
- 此前我们在前端和后端分别维护两份清体力副本数据，非常容易导致数据不一致的问题。
- 现在我们统一了数据源，前端启动时会从后端获取最新的清体力副本数据，确保前后端数据一致。
- 如过您发现更新后前端下拉列表中缺少某些副本，或完全为空，请及时与开发者反馈，我们会尽快修复。

---

## 2.18 更新公告

亲爱的SRA用户：

2.18版本已完成修复并发布了2.18.2。我们对给您带来的不便深表歉意，并感谢您的理解与支持。

#### 主要更新内容:
- SRA-Server 新增 API Key 认证，保护服务器安全。
- SRA-cli 已发布到 PyPI，可直接安装使用。
- 新增抽卡资源预测扩展，可根据当前资源、奖励指南、版本周期、月卡、深渊类刷新、周常奖励和前瞻兑换码估算当前版本结束前可获得的抽卡资源。 #208
- 新增获取任务状态功能，可查询当前任务状态。
- 更换rapidocr\_onnxruntime库为rapidocr。
- 适配4.4版本「鸣笛于归寂之时」

#### 功能调整：
- 更新了问候语。
- 移除废弃的 `CurrencyWarsPolicy` 配置项。

#### 问题修复：
- 修复了b服的登录问题。
- 修复了版本更新后遗器刷取错位的问题。

#### 更新说明：

###### SRA-Server
- 新增基于密钥的认证机制：在 `appsettings.json` 中配置 `AccessToken` 后启用，未配置时允许匿名访问。
- 认证方式：请求头添加 `X-Access-Token: <your-token>`。

###### 获取任务状态
- 新增获取任务状态功能，可查询当前任务状态。
  cli: `sra-cli task status`
  server: GET `/api/task/status`

---

## 2.17 更新公告

#### 主要更新内容:
- 记忆窗口大小和位置：在设置中启用后，SRA会记住您上次关闭窗口时的大小和位置。
- SRA-server：新增了一个新的服务端应用，您可以通过http协议与SRA进行通信。
- 远程后端支持：桌面端可以通过HTTP连接远程SRA服务端，实现跨设备任务管理。

#### 功能调整：
- 更新了任务结束后通知的截图页面，增加截图信息量。 #198
- 更新了问候语。

#### 问题修复：
- 修复了SRA无法将游戏窗口移动到前台的问题。
- 修复了云游戏模式启动游戏任务中途失败的问题。
- 执行体力清理时被切换视角的提示卡住
- 修复了无名勋礼更新时卡在奖励预览页面的问题。
- 修复了首次使用SRA时，报错提示未找到settings.json的问题。

#### 更新说明：

###### SRA-server
- 新增了一个新的服务端应用，您可以通过http协议与SRA进行通信。
- 服务端应用的端口号为5000，通过`--port`选项可以自定义端口号。
- 访问`http://localhost:5000/swagger`可以查看API文档。

###### 远程后端
- 桌面端新增远程后端模式，可通过HTTP连接远程SRA服务端执行任务。
- 在高级设置中可启用远程后端并配置服务器地址（默认 `http://localhost:5000`）。
- 连接后自动订阅服务端SSE日志流，实时显示远程任务输出。
- 支持通过 `Task/run` 和 `Task/stop` API 启动和停止远程任务。
- SSE断线后自动重连（3秒间隔）。

---

## 2.16 更新公告

#### 主要更新内容:
- 适配4.3版本：
  - 「侵蚀隧洞•观火之径」「凝滞虚影•吞噬之形」「智识之蕾•海原电视塔」「毁灭之蕾•渡画泉隐」
- SRA-cli 不再强制要求UAC权限，但默认情况下启动时会自动以管理员权限重新运行。如果你想以普通用户权限运行，请使用`--no-admin`选项。
- SRA-cli 支持在调用应用时通过将命令作为额外参数传递给程序来发送命令。
- SRA-cli 支持启动脚本，您可以在当前目录下创建一个名为`.srarc`的文件，用于在启动时自动执行的命令。
- 任务失败时，会自动截图并保存到`log/screenshot`目录下。
- 货币战争刷开局投资策略支持同一位面设置多个策略，满足其中一个策略条件即可执行。

#### 功能调整：
- 移除了启动命令run和single。

#### 更新说明：

###### 破坏性变更：
- 移除了启动命令run和single。
  - 我们在2.11版本中已将这两个命令标记为过时，现在它们已被移除。您可以使用`-e`选项或使用启动命令来替代。
- 移除了`--embed`选项，请使用`--inline`选项

###### 启动命令
- SRA-cli 支持在调用应用时通过将命令作为额外参数传递给程序来发送命令。
- 示例：SRA-cli "run Default" exit

###### 启动脚本
- SRA-cli 支持启动脚本，您可以在当前目录下创建一个名为`.srarc`的文件，用于在启动时自动执行的命令。
- 示例：
  ```
  run Default
  exit
  ```
  - 这将在启动SRA后立即阻塞式运行"Default"配置文件，并在完成后执行"exit"命令退出SRA。

###### 货币战争刷开局投资策略支持同一位面设置多个策略
- 此前，货币战争刷开局投资策略只能设置一个策略。现在，您可以在同一位面设置多个策略，只要满足其中一个策略的条件，就会执行下一步操作。
- 例如："白银投资 溜佩佩 招财狗；"表示在第一位面的投资策略出现"白银投资"或"溜佩佩"或"招财狗"中的任意一个即可执行下一步操作。

---

## 2.15 更新公告

#### 主要更新内容:
- 现在可以设置在每个任务开始前和结束后分别进行通知了。
- 如果在背景图片路径中输入"shasnow"的话会发生什么呢?

#### 功能调整：
- 重构了通知系统，使其更加灵活和易于配置。
- 移除了部分过时的设置选项。

#### 问题修复：
- 修复了通知配置无效的问题。
- 修复了无名勋礼存在奖励不领取的问题。
- 修复了货币战争中当等级达到10级时仍继续尝试升级的问题。
- 修复了凝滞虚影"纷争先兆"无法被正确识别的问题。
- 修复了导入格式错误的攻略文件时程序崩溃的问题。

---

## 2.14 更新公告

#### 主要更新内容:
- 更新 .NET 10.0
- 自动开启自动战斗。需要启用游戏启动参数
- 差分宇宙新增'启用积分奖励'功能。启用后当每周积分达到上限时将跳过差分宇宙任务，并自动领取积分奖励。
- 清体力任务新增"启用多倍活动检测"功能。启用后将检测当前是否有花藏繁生、异器盈界等活动，并优先刷对应的副本。

#### 功能调整：
- 清体力任务配置界面更新。

#### 问题修复：
- 修复了货币战争刷开局阶段1没有刷到需要的投资策略时仍继续往下打的问题。
- 修复了云星穹铁道当cookies失效时无法登录的问题。
- 修复签证奖励无法领取的问题。
- 修复了自动计算饰品提取挑战次数时没有计入沉浸器的问题。
- 修复了使用本地python创建虚拟环境时失败的问题。

#### 更新说明：

你必须安装.NET 10.0 Desktop Runtime才能运行更新后的SRA

任务配置文件格式已更新，旧版本的任务配置文件将无法直接使用，更新后请使用SRA重新打开任务配置，SRA会自动将其转换为新的格式。

###### 启用多倍活动检测
- 该功能将检测当前是否有花藏繁生、异器盈界等活动，并优先刷对应的副本。
- 需要在清体力任务配置界面启用。
- 开启后，在清体力时将跳转到活动页面检测当前是否有多倍活动，如果有则优先刷对应的副本。
- 实现方式是将对应的副本添加到自动检测的副本列表头部，因此培养目标或手动添加的自动检测任务仍然有效。

###### 更新 .NET 10.0：
- 本次更新将更新SRA的.NET运行环境为10.0，这将带来性能提升和新的功能。
- 请确保你的系统已安装.NET 10.0 Desktop Runtime，否则SRA将无法正常运行。
- [安装.NET 10.0 Desktop Runtime](https://builds.dotnet.microsoft.com/dotnet/WindowsDesktop/10.0.7/windowsdesktop-runtime-10.0.7-win-x64.exe)

---

## 2.13 更新公告

#### 主要更新内容:
- 适配云·星穹铁道（测试版）
- 货币战争刷开局支持两个位面分别刷不同的投资策略
- 在拓展中添加了猫猫糕友人帐功能，可以帮助你收集需要的猫猫糕。由catcake.hoshimi.io提供支持。

#### 功能调整：
- 重构了设置模块，已启用新的设置格式，本次更新将自动迁移旧设置到新格式。
- 货币战争刷开局投资策略输入格式调整。
- 移除了货币战争刷开局策略阶段配置。

#### 问题修复：
- 修复了货币战争刷开局在第一位面选择投资策略时错误重开的问题。
- 修复任务结束后关机、休眠、退出SRA等操作无效的问题。
- 修复了专家邀请函无法正确处理的问题。

#### 更新说明：

###### 云·星穹铁道适配：
- 在设置中启用，目前仅支持使用Microsoft Edge浏览器，确保你的系统中已安装Microsoft Edge浏览器。
- 必须勾选启动游戏任务，并填写账号和密码
- 推荐在有畅玩卡或云游戏空闲的情况下使用
- 已知问题：
  - 任务结束后浏览器不会自动关闭，需要手动关闭浏览器窗口。
  - 目前仅支持使用Microsoft Edge浏览器，其他浏览器可能无法正常工作。
  - 可能会遇到登录问题，确保账号和密码正确，并且网络连接稳定。
  - 自动战斗和倍速未开启。

###### 货币战争刷开局投资策略输入格式调整：
- 现在支持在输入框中输入两个位面分别的投资策略，使用分号分隔，格式为：`位面1投资策略;位面2投资策略`，支持中文分号。
- 例如：`轮回不止；环保大使叽米` 表示在第一位面刷取"轮回不止"策略，在第二位面刷取"环保大使叽米"策略。
- 前一个策略满足条件后才会开始刷取后一个策略。
- 如果只输入一个策略，则默认在第一位面刷取该策略，第二位面不刷取任何策略。
- 如果想要在第二位面刷取策略但不在第一位面刷取任何策略，可以输入`;位面2投资策略`，例如：`;环保大使叽米`。
